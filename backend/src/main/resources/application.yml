spring:
  config:
    import:
      - classpath:/yaml/application-prod.yml
#      - optional:classpath:/yaml/application-local.yml
      - optional:classpath:/yaml/application-test.yml

  profiles:
    active: local

server:
  port: 7070

logging:
  level:
    root: INFO
