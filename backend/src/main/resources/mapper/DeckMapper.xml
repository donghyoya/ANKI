<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kmu.anki.backend.domain.card.repository.DeckMapper">

    <!-- 쿼리: 난이도별 카드 통계 조회 -->
    <select id="findDeckByDifficulty" parameterType="java.lang.Long" resultType="com.kmu.anki.backend.domain.card.dto.DeckDto">
        SELECT
        korean_cards.level AS category,
        COUNT(korean_card_id) AS cardsCounts,
        SUM(CASE WHEN user_cards.due &lt;= NOW() THEN 1 ELSE 0 END) AS overdueCounts,
        SUM(CASE WHEN user_cards.user_card_state = 'review' THEN 1 ELSE 0 END) AS maturityCounts
        FROM korean_cards
        JOIN user_cards USING (korean_card_id)
        WHERE user_cards.user_id = #{userId}
        GROUP BY korean_cards.level
    </select>

    <!-- 쿼리: 주제별 카드 통계 조회 -->
    <select id="findDeckByMeaning" parameterType="java.lang.Long" resultType="com.kmu.anki.backend.domain.card.dto.DeckDto">
        SELECT
        card_topics.topic_id AS category,
        COUNT(korean_card_id) AS cardsCounts,
        SUM(CASE WHEN user_cards.due &lt;= NOW() THEN 1 ELSE 0 END) AS overdueCounts,
        SUM(CASE WHEN user_cards.user_card_state = 'review' THEN 1 ELSE 0 END) AS maturityCounts
        FROM korean_cards
        JOIN user_cards USING (korean_card_id)
        JOIN card_topics USING (korean_card_id)
        WHERE user_cards.user_id = #{userId}
        GROUP BY card_topics.topic_id
    </select>

    <!-- 쿼리: 주제별 카드 통계 조회 -->
    <select id="findDeckByMeaningWithoutUser" resultType="com.kmu.anki.backend.domain.card.dto.DeckDto">
        SELECT
            card_topics.topic_id AS category,
            COUNT(korean_card_id) AS cardsCounts
        FROM korean_cards
                 JOIN card_topics USING (korean_card_id)
        GROUP BY card_topics.topic_id
    </select>

</mapper>
